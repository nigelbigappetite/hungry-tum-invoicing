# =============================================================================
# Hungry Tum Invoicing – Environment variables (for local .env.local or Vercel)
# =============================================================================
# Copy to .env.local for local dev. For Vercel: Project → Settings → Environment Variables.
# Do not commit .env.local or real secrets.
# =============================================================================

# --- Required (Supabase) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# --- Required for BACS / Stripe / webhooks ---
STRIPE_SECRET_KEY=sk_live_... or sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # Supabase Dashboard → Settings → API → service_role

# --- App URL (required for production BACS links and redirects) ---
# Set to your Vercel URL or custom domain (no trailing slash)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# --- Invoice PDF (optional) ---
INVOICE_PAYMENT_DAYS=7
INVOICE_BANK_NAME=Your Bank Name
INVOICE_SORT_CODE=12-34-56
INVOICE_ACCOUNT_NUMBER=12345678
# Business address shown on invoice (issuer / "From"). Use \n for line breaks.
# INVOICE_BUSINESS_ADDRESS=Hungry Tum Ltd\n123 Example Street\nCity\nPostcode
INVOICE_BUSINESS_ADDRESS=

# --- Email (BACS setup + optional invoice emails) ---
RESEND_API_KEY=re_...
BACS_EMAIL_FROM=Hungry Tum <bacs@yourdomain.com>
# INVOICE_EMAIL_FROM=Hungry Tum <invoices@yourdomain.com>   # optional, defaults to BACS_EMAIL_FROM

# --- Optional (dev only) ---
# CREATE_TEST_USER_SECRET=any-secret   # only if using /api/create-test-user
